'use strict';

// Ash Turbo - Background Service Worker
// Minimal implementation for the high-speed version.

chrome.runtime.onInstalled.addListener(() => {
  console.log('Ash Turbo installed.');
});
